// Add bouncing fields to form state
bouncing3Months: '', bouncing6Months: '',

// Add to form submission
bouncing_3_months: Number(form.bouncing3Months) || 0,
bouncing_6_months: Number(form.bouncing6Months) || 0,

// Add to form JSX (after income details section)
{/* Bouncing Details */}
<div className="mt-4 p-4 rounded-lg bg-muted/20 border border-muted">
  <h3 className="text-sm font-semibold text-foreground mb-3">Banking History</h3>
  <div className="grid grid-cols-2 gap-3">
    <div className="floating-input-wrapper">
      <input type="number" min="0" className={inputClass} value={form.bouncing3Months} onChange={e => update('bouncing3Months', e.target.value)} placeholder=" " />
      <label className={labelClass}>Bouncing in 3 Months</label>
    </div>
    <div className="floating-input-wrapper">
      <input type="number" min="0" className={inputClass} value={form.bouncing6Months} onChange={e => update('bouncing6Months', e.target.value)} placeholder=" " />
      <label className={labelClass}>Bouncing in 6 Months</label>
    </div>
  </div>
</div>
